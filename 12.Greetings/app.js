// message should be personalized with the person’s name.
var member = ['areeba', 'ayesha', 'nashra', 'mayesa'];
var message = 'Keep shining bright and never doubt your potential:';
for (var i = 0; i < member.length; i++) {
    console.log(message + member[i]);
}
